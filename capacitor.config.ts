import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'final-project-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
